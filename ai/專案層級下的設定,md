## 專案MD檔
在專業的軟體開發流程中，除了 `README.md`，還有許多專門用途的 `.md` 檔案。這些檔案構成了專案的 **「治理架構」**。我將它們分為四大類：

### 1. 核心文檔 (Core Docs)
| 檔名 | 用途描述 | 我（AI）會參考的點 |
| :--- | :--- | :--- |
| **`README.md`** | 專案的主入口。包含簡介、環境設定與啟動清單。 | 專案的全域背景與技術棧。 |
| **`CONTRIBUTING.md`** | 指導他人如何貢獻代碼、PR 規範與 Git 分支模型。 | 提交代碼的格式與開發流程。 |
| **`CHANGELOG.md`** | 紀錄各版本的更新日誌內容與修復事項。 | 回溯過去的修改紀錄與版本歷史。 |

### 2. 開發規範 (Guidelines & Specs)
| 檔名 | 用途描述 | 我（AI）會參考的點 |
| :--- | :--- | :--- |
| **`GUIDELINES.md`** | 自定義的代碼風格、目錄結構或專案特定禁忌。 | 您對代碼品質的「主觀要求」。 |
| **`CUSTOM_SKILL.md`** | 針對特定腳本或工具的撰寫技術規範。 | 確保工具代碼外觀與運作一致。 |
| **`ARCHITECT.md`** | 描述系統架構設計（如領域驅動設計 DDD 結構）。 | 決定新功能的檔案放置位置。 |
| **`SECURITY.md`** | 安全漏洞報告流程、支援的安全版本與政策。 | 權限控管與安全性設計邏輯。 |

### 3. 專案管理 (Project Management)
| 檔名 | 用途描述 | 我（AI）會參考的點 |
| :--- | :--- | :--- |
| **`TASK.md`** | 目前正在進行的 TODO 清單與任務進度。 | 決定下一輪對話的優先順序。 |
| **`PLAN.md`** | 大型功能的實作計畫、技術選型決策（ADR）。 | 確保執行複雜任務時不跳過關鍵步驟。 |
| **`WALKTHROUGH.md`** | 當前工作成果的自測報告與驗證過程。 | 幫助您快速審閱我剛才完成的變動。 |

### 4. 法律與合規 (Legal)
| 檔名 | 用途描述 | 我（AI）會參考的點 |
| :--- | :--- | :--- |
| **`LICENSE.md`** | 版權聲明與使用授權（如 MIT, Apache, Proprietary）。 | 確認是否能引用某些開源代碼。 |
| **`CODE_OF_CONDUCT.md`** | 社群行為準則，規範開發者之間的互動。 | 溝通時的語氣與專業規範。 |

---

### 💡 實務建議
在您的 `PS1` 掃描工具專案中，目前已經具備了：
1.  **README.md** (基礎)
2.  **GUIDELINES.md** (通則)
3.  **CUSTOM_SKILL.md** (細則)

## 其他常用的設定檔
除了我們自定義的檔案外，作為 Antigravity AI，我還會主動辨識並參考目錄中各種「標準」的專案層級設定檔。這些檔案在現代開發流程中具有固定的意義，我會依據它們來調整我的工作行為。

以下是常見專案層級檔案的參考對照表：

| 檔案類型 | 常見檔名 | 我會參考的內容與用途 |
| :--- | :--- | :--- |
| **文件與規範** | `README.md` | 專案背景、安裝步驟、核心功能描述。這是我理解專案的第一來源。 |
| **環境與安全** | `.env`, `.env.example` | 偵測專案使用了哪些環境變數（但我會避免外洩機密），決定變數讀取模式。 |
| **Git 忽略規則** | `.gitignore` | 我在搜尋、列出目錄或執行掃描時，會主動跳過這些被忽略的檔案。 |
| **程式碼風格** | `.editorconfig` | 自動調整縮排（Tab 或 Space）、換行符號（LF/CRLF）等排版細節。 |
| **依賴管理** | `package.json`, `pom.xml`, `.csproj` | 判斷專案使用的語言版本、框架與第三方套件，據此給出精準的代碼建議。 |
| **Lint/校驗規則** | `.eslintrc`, `.markdownlint.json` | 在修改檔案前，我會參考這些校驗規則，確保我寫出的代碼不會噴紅字。 |
| **AI 記憶與計畫** | `task.md`, `implementation_plan.md` | 我會讀取這些檔案來接續之前的開發進度，了解「下一步要做什麼」。 |
| **CI/CD 流程** | `.github/workflows/*.yml` | 了解專案的自動化測試與部署流程，確保修改不會弄斷 Build。 |

### 💡 如何利用這些檔案來控制AI Agent？
如果您希望我在接下來的對話中展現特定的行為，您可以隨時建立或修改這些檔案。

*   **範例 1**：如果您在 `.editorconfig` 規定使用 4 個空格縮排，我寫代碼時就不會用 Tab。
*   **範例 2**：透過維護 `task.md`，您可以隨時掌握我目前處理到哪一個步驟，而不需要一直詢問我。

總結來說，我是一個 **「高度依賴環境 Context」** 的助手，您的目錄結構越完整（README、Gitignore、Linter），我的表現就會越像一個經驗豐富的資深工程師。

> 文章內的 "我" 意思是 AI Agent